#pragma once

// Copy this file to secrets.h and fill in real values.
// secrets.h is gitignored — never commit real credentials.

#define WIFI_SSID "YOUR_WIFI"
#define WIFI_PASSWORD "YOUR_PASSWORD"

#define DEVICE_ID "YOUR_DEVICE_UUID"
#define DEVICE_KEY "YOUR_DEVICE_SECRET"

// Production: https://your-app.vercel.app (no trailing slash)
// Local LAN testing: http://192.168.x.x:3000
#define API_BASE_URL "https://your-domain.com"
